import {proxyHandler} from '@openint/api'

export {
  /* @next-codemod-error `proxyHandler` export is re-exported. Check if this component uses `params` or `searchParams`*/
  proxyHandler as GET,
  /* @next-codemod-error `proxyHandler` export is re-exported. Check if this component uses `params` or `searchParams`*/
  proxyHandler as POST,
  /* @next-codemod-error `proxyHandler` export is re-exported. Check if this component uses `params` or `searchParams`*/
  proxyHandler as PUT,
  /* @next-codemod-error `proxyHandler` export is re-exported. Check if this component uses `params` or `searchParams`*/
  proxyHandler as DELETE,
  /* @next-codemod-error `proxyHandler` export is re-exported. Check if this component uses `params` or `searchParams`*/
  proxyHandler as OPTIONS,
  /* @next-codemod-error `proxyHandler` export is re-exported. Check if this component uses `params` or `searchParams`*/
  proxyHandler as PATCH,
}
