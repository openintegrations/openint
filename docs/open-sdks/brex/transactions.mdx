---
openapi: https://raw.githubusercontent.com/openintegrations/openSDKs/refs/heads/main/sdks/sdk-brex/brex_transactions.oas.json
---

## Integrating with Brex OpenSDK 

Welcome to the Brex OpenSDK documentation. OpenSDKs provides a type-safe and standardized way to interact with various APIs, ensuring a consistent and efficient integration experience.

The Brex OpenSDK allows you to easily integrate with the Brex API, providing a robust set of tools to manage your API interactions with managed auth by OpenInt. 

### Available Methods:

| Method | Path | Description |
|--------|------|-------------|
| **GET** | /v2/accounts/card |  List card accounts 
| **GET** | /v2/accounts/card/primary/statements |  List primary card account statements.  
| **GET** | /v2/accounts/cash |  List cash accounts 
| **GET** | /v2/accounts/cash/primary |  Get primary cash account 
| **GET** | /v2/accounts/cash/{id} |  Get cash account by ID 
| **GET** | /v2/accounts/cash/{id}/statements |  List cash account statements.     
| **GET** | /v2/transactions/card/primary |  List transactions for all card accounts. 
| **GET** | /v2/transactions/cash/{id} |  List transactions for the selected cash account. 


## Code Example

```typescript

    import {initSDK} from '@opensdks/runtime'
    import {brexSdkDef} from '@opensdks/sdk-brex'

    const brex = initSDK(brexSdkDef, {
      auth: {
        // for provider-specific auth
        bearer: `${process.env['BREX_TOKEN']}`,
        // or for passthrough auth via OPENINT_API_KEY & end user resourceId
        openInt: {
          apiKey: `${process.env['OPENINT_API_KEY']}`,
          resourceId: END_USER_RESOURCE_ID,
        }
      }, params: { ... }
    })

    //  List card accounts 
    const res = await brex.GET('/v2/accounts/card')
```
