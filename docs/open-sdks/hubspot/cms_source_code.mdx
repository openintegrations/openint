---
openapi: https://raw.githubusercontent.com/openintegrations/openSDKs/refs/heads/main/sdks/sdk-hubspot/hubspot_cms_source_code.oas.json
---

## Integrating with Hubspot OpenSDK 

Welcome to the Hubspot OpenSDK documentation. OpenSDKs provides a type-safe and standardized way to interact with various APIs, ensuring a consistent and efficient integration experience.

The Hubspot OpenSDK allows you to easily integrate with the Hubspot API, providing a robust set of tools to manage your API interactions with managed auth by OpenInt. 

### Available Methods:

| Method | Path | Description |
|--------|------|-------------|
| **GET** | /cms/v3/source-code/{environment}/content/{path} | Download a file
| **PUT** | /cms/v3/source-code/{environment}/content/{path} | Create or update a file
| **POST** | /cms/v3/source-code/{environment}/content/{path} | Create a file
| **DELETE** | /cms/v3/source-code/{environment}/content/{path} | Delete a file
| **GET** | /cms/v3/source-code/{environment}/metadata/{path} | Get the metadata for a file
| **POST** | /cms/v3/source-code/{environment}/validate/{path} | Validate the contents of a file
| **POST** | /cms/v3/source-code/extract/{path} | Extracts a zip file
| **POST** | /cms/v3/source-code/extract/async | 
| **GET** | /cms/v3/source-code/extract/async/tasks/{taskId}/status | 


## Code Example

```typescript

    import {initSDK} from '@opensdks/runtime'
    import {hubspotSdkDef} from '@opensdks/sdk-hubspot'

    const hubspot = initSDK(hubspotSdkDef, {
      auth: {
        // for provider-specific auth
        bearer: `${process.env['HUBSPOT_TOKEN']}`,
        // or for passthrough auth via OPENINT_API_KEY & end user resourceId
        openInt: {
          apiKey: `${process.env['OPENINT_API_KEY']}`,
          resourceId: END_USER_RESOURCE_ID,
        }
      }, params: { ... }
    })

    // Download a file
    const res = await hubspot.GET('/cms/v3/source-code/{environment}/content/{path}')
```
