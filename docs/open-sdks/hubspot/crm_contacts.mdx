---
openapi: https://raw.githubusercontent.com/openintegrations/openSDKs/refs/heads/main/sdks/sdk-hubspot/hubspot_crm_contacts.oas.json
---

## Integrating with Hubspot OpenSDK 

Welcome to the Hubspot OpenSDK documentation. OpenSDKs provides a type-safe and standardized way to interact with various APIs, ensuring a consistent and efficient integration experience.

The Hubspot OpenSDK allows you to easily integrate with the Hubspot API, providing a robust set of tools to manage your API interactions with managed auth by OpenInt. 

### Available Methods:

| Method | Path | Description |
|--------|------|-------------|
| **POST** | /crm/v3/objects/contacts/batch/read | Read a batch of contacts by internal ID, or unique property values
| **GET** | /crm/v3/objects/contacts/{contactId} | Read
| **DELETE** | /crm/v3/objects/contacts/{contactId} | Archive
| **PATCH** | /crm/v3/objects/contacts/{contactId} | Update
| **POST** | /crm/v3/objects/contacts/merge | Merge two contacts with same type
| **POST** | /crm/v3/objects/contacts/batch/archive | Archive a batch of contacts by ID
| **POST** | /crm/v3/objects/contacts/batch/create | Create a batch of contacts
| **POST** | /crm/v3/objects/contacts/batch/update | Update a batch of contacts
| **POST** | /crm/v3/objects/contacts/gdpr-delete | GDPR DELETE
| **GET** | /crm/v3/objects/contacts | List
| **POST** | /crm/v3/objects/contacts | Create
| **POST** | /crm/v3/objects/contacts/search | 


## Code Example

```typescript

    import {initSDK} from '@opensdks/runtime'
    import {hubspotSdkDef} from '@opensdks/sdk-hubspot'

    const hubspot = initSDK(hubspotSdkDef, {
      auth: {
        // for provider-specific auth
        bearer: `${process.env['HUBSPOT_TOKEN']}`,
        // or for passthrough auth via OPENINT_API_KEY & end user resourceId
        openInt: {
          apiKey: `${process.env['OPENINT_API_KEY']}`,
          resourceId: END_USER_RESOURCE_ID,
        }
      }, params: { ... }
    })

    // Read a batch of contacts by internal ID, or unique property values
    const res = await hubspot.POST('/crm/v3/objects/contacts/batch/read')
```
