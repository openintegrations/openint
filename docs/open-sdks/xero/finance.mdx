---
openapi: https://raw.githubusercontent.com/openintegrations/openSDKs/refs/heads/main/sdks/sdk-xero/xero_finance.oas.json
---

## Integrating with Xero OpenSDK 

Welcome to the Xero OpenSDK documentation. OpenSDKs provides a type-safe and standardized way to interact with various APIs, ensuring a consistent and efficient integration experience.

The Xero OpenSDK allows you to easily integrate with the Xero API, providing a robust set of tools to manage your API interactions with managed auth by OpenInt. 

### Available Methods:

| Method | Path | Description |
|--------|------|-------------|
| **PARAMETERS** | /CashValidation | 
| **GET** | /CashValidation | Get cash validation
| **PARAMETERS** | /AccountingActivities/AccountUsage | 
| **GET** | /AccountingActivities/AccountUsage | Get account usage
| **PARAMETERS** | /AccountingActivities/LockHistory | 
| **GET** | /AccountingActivities/LockHistory | Get lock history
| **PARAMETERS** | /AccountingActivities/ReportHistory | 
| **GET** | /AccountingActivities/ReportHistory | Get report history
| **PARAMETERS** | /AccountingActivities/UserActivities | 
| **GET** | /AccountingActivities/UserActivities | Get user activities
| **PARAMETERS** | /FinancialStatements/BalanceSheet | 
| **GET** | /FinancialStatements/BalanceSheet | Get Balance Sheet report
| **PARAMETERS** | /FinancialStatements/Cashflow | 
| **GET** | /FinancialStatements/Cashflow | Get Cash flow report
| **PARAMETERS** | /FinancialStatements/ProfitAndLoss | 
| **GET** | /FinancialStatements/ProfitAndLoss | Get Profit & Loss report
| **PARAMETERS** | /FinancialStatements/TrialBalance | 
| **GET** | /FinancialStatements/TrialBalance | Get Trial Balance report
| **PARAMETERS** | /FinancialStatements/contacts/revenue | 
| **GET** | /FinancialStatements/contacts/revenue | Get revenue by contacts report
| **PARAMETERS** | /FinancialStatements/contacts/expense | 
| **GET** | /FinancialStatements/contacts/expense | Get expense by contacts report
| **PARAMETERS** | /BankStatementsPlus/statements | 
| **GET** | /BankStatementsPlus/statements | Get Bank Statement Accounting


## Code Example

```typescript

    import {initSDK} from '@opensdks/runtime'
    import {xeroSdkDef} from '@opensdks/sdk-xero'

    const xero = initSDK(xeroSdkDef, {
      auth: {
        // for provider-specific auth
        bearer: `${process.env['XERO_TOKEN']}`,
        // or for passthrough auth via OPENINT_API_KEY & end user resourceId
        openInt: {
          apiKey: `${process.env['OPENINT_API_KEY']}`,
          resourceId: END_USER_RESOURCE_ID,
        }
      }
    })

    
    const res = await xero.PARAMETERS('/CashValidation')
```
