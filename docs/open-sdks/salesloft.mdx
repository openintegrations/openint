---
openapi: https://raw.githubusercontent.com/openintegrations/openSDKs/refs/heads/main/sdks/sdk-salesloft/salesloft.oas.json
---

## Integrating with Salesloft OpenSDK 

Welcome to the Salesloft OpenSDK documentation. OpenSDKs provides a type-safe and standardized way to interact with various APIs, ensuring a consistent and efficient integration experience.

The Salesloft OpenSDK allows you to easily integrate with the Salesloft API, providing a robust set of tools to manage your API interactions with managed auth by OpenInt. 

### Available Methods:

| Method | Path | Description |
|--------|------|-------------|
| **GET** | /v2/account_stages.json | List account stages
| **GET** | /v2/account_stages/{id}.json | Fetch an account stage
| **GET** | /v2/account_tiers.json | List Account Tiers
| **GET** | /v2/account_tiers/{id}.json | Fetch an account tier
| **POST** | /v2/account_upserts.json | Upsert an account
| **GET** | /v2/accounts.json | List accounts
| **POST** | /v2/accounts.json | Create an account
| **DELETE** | /v2/accounts/{id}.json | Delete an account
| **GET** | /v2/accounts/{id}.json | Fetch an account
| **PUT** | /v2/accounts/{id}.json | Update an existing Account
| **GET** | /v2/action_details/call_instructions.json | List call instructions
| **GET** | /v2/action_details/call_instructions/{id}.json | Fetch a call instructions
| **GET** | /v2/actions.json | List actions
| **GET** | /v2/actions/{id}.json | Fetch an action
| **POST** | /v2/activities.json | Create an activity
| **GET** | /v2/activities/calls.json | List calls
| **POST** | /v2/activities/calls.json | Create a call
| **GET** | /v2/activities/calls/{id}.json | Fetch a call
| **GET** | /v2/activities/emails.json | List emails
| **GET** | /v2/activities/emails/{id}.json | Fetch an email
| **GET** | /v2/activity_histories | List Past Activities
| **GET** | /v2/bulk_jobs | List bulk jobs
| **POST** | /v2/bulk_jobs | Create a bulk job
| **GET** | /v2/bulk_jobs/{bulk_jobs_id}/job_data | List job data for a bulk job
| **POST** | /v2/bulk_jobs/{bulk_jobs_id}/job_data | Create job data for a bulk job
| **GET** | /v2/bulk_jobs/{bulk_jobs_id}/results | List job data for a completed bulk job.
| **GET** | /v2/bulk_jobs/{id} | Fetch a bulk job
| **PUT** | /v2/bulk_jobs/{id} | Update a bulk job
| **GET** | /v2/cadence_memberships.json | List cadence memberships
| **POST** | /v2/cadence_memberships.json | Create a cadence membership
| **DELETE** | /v2/cadence_memberships/{id}.json | Delete a cadence membership
| **GET** | /v2/cadence_memberships/{id}.json | Fetch a cadence membership
| **GET** | /v2/cadences.json | List cadences
| **GET** | /v2/cadences/{id}.json | 
| **GET** | /v2/calendar/events | List calendar events
| **POST** | /v2/calendar/events/upsert | Upsert a calendar event
| **GET** | /v2/call_data_records.json | List call data records
| **GET** | /v2/call_data_records/{id}.json | Fetch a call data record
| **GET** | /v2/call_dispositions.json | List call dispositions
| **GET** | /v2/call_sentiments.json | List call sentiments
| **POST** | /v2/conversations/calls | Create Conversations Call
| **GET** | /v2/crm_activities.json | List crm activities
| **GET** | /v2/crm_activities/{id}.json | Fetch a crm activity
| **GET** | /v2/crm_activity_fields.json | List crm activity fields
| **GET** | /v2/crm_users.json | List crm users
| **GET** | /v2/custom_fields.json | List custom fields
| **POST** | /v2/custom_fields.json | Create a custom field
| **DELETE** | /v2/custom_fields/{id}.json | Delete a custom field
| **GET** | /v2/custom_fields/{id}.json | Fetch a custom field
| **PUT** | /v2/custom_fields/{id}.json | Update a custom field
| **GET** | /v2/email_template_attachments.json | List email template attachments
| **GET** | /v2/email_templates.json | List email templates
| **GET** | /v2/email_templates/{id}.json | Fetch an email template
| **POST** | /v2/external_emails.json | Create an External Email
| **GET** | /v2/groups.json | List groups
| **GET** | /v2/groups/{id}.json | Fetch a group
| **GET** | /v2/imports.json | List imports
| **POST** | /v2/imports.json | Create an import
| **DELETE** | /v2/imports/{id}.json | Delete an import
| **GET** | /v2/imports/{id}.json | Fetch an import
| **PUT** | /v2/imports/{id}.json | Update an import
| **POST** | /v2/live_website_tracking_parameters.json | Create an Live Website Tracking Parameter
| **GET** | /v2/me.json | Fetch current user
| **GET** | /v2/meetings.json | List meetings
| **POST** | /v2/meetings/settings/searches.json | List meeting settings
| **PUT** | /v2/meetings/settings/{id}.json | Update a meeting setting
| **PUT** | /v2/meetings/{id}.json | Update a meeting
| **GET** | /v2/mime_email_payloads/{id}.json | Fetch the MIME content for email
| **GET** | /v2/notes.json | List notes
| **POST** | /v2/notes.json | Create a note
| **DELETE** | /v2/notes/{id}.json | Delete a note
| **GET** | /v2/notes/{id}.json | Fetch a note
| **PUT** | /v2/notes/{id}.json | Update a note
| **POST** | /v2/ongoing_actions.json | Create an ongoing action
| **GET** | /v2/pending_emails.json | Fetches a list of emails ready to be sent by an external email service. Only emails sent with an External Email Client will appear here.
| **PUT** | /v2/pending_emails/{id}.json | Updates the status of an email sent by an External Email Client
| **GET** | /v2/people.json | List people
| **POST** | /v2/people.json | Create a person
| **DELETE** | /v2/people/{id}.json | Delete a person
| **GET** | /v2/people/{id}.json | Fetch a person
| **PUT** | /v2/people/{id}.json | Update a person
| **GET** | /v2/person_stages.json | List person stages
| **POST** | /v2/person_stages.json | Create a person stage
| **DELETE** | /v2/person_stages/{id}.json | Delete an person stage
| **GET** | /v2/person_stages/{id}.json | Fetch a person stage
| **PUT** | /v2/person_stages/{id}.json | Update a person stage
| **POST** | /v2/person_upserts.json | Upsert a person
| **GET** | /v2/phone_number_assignments.json | List phone number assignments
| **GET** | /v2/phone_number_assignments/{id}.json | Fetch a phone number assignment
| **GET** | /v2/phone_numbers/caller_ids.json | List caller ids
| **GET** | /v2/phone_numbers/recording_settings/{id}.json | Fetch recording setting
| **GET** | /v2/saved_list_views.json | List saved list views
| **POST** | /v2/saved_list_views.json | Create a saved list view
| **DELETE** | /v2/saved_list_views/{id}.json | Delete a saved list view
| **GET** | /v2/saved_list_views/{id}.json | Fetch a saved list view
| **PUT** | /v2/saved_list_views/{id}.json | Update a saved list view
| **GET** | /v2/steps.json | List steps
| **GET** | /v2/steps/{id}.json | Fetch a step
| **GET** | /v2/successes.json | List successes
| **GET** | /v2/tags.json | List team tags
| **GET** | /v2/tasks.json | List tasks
| **POST** | /v2/tasks.json | Create a Task
| **GET** | /v2/tasks/{id}.json | Fetch a task
| **PUT** | /v2/tasks/{id}.json | Update a Task
| **GET** | /v2/team.json | Fetch current team
| **GET** | /v2/team_template_attachments.json | List team template attachments
| **GET** | /v2/team_templates.json | List team templates
| **GET** | /v2/team_templates/{id}.json | Fetch a team template
| **POST** | /v2/third_party_live_feed_items | Create a live feed item
| **GET** | /v2/users.json | List users
| **GET** | /v2/users/{id}.json | Fetch a user
| **GET** | /v2/webhook_subscriptions | List webhook subscriptions
| **POST** | /v2/webhook_subscriptions | Create a webhook subscription
| **DELETE** | /v2/webhook_subscriptions/{id} | Delete a webhook subscription
| **GET** | /v2/webhook_subscriptions/{id} | Fetch a webhook subscription
| **PUT** | /v2/webhook_subscriptions/{id} | Update a webhook subscription
| **POST** | /v2/cadence_imports | 
| **GET** | /v2/cadence_exports/{id} | 


## Code Example

```typescript

    import {initSDK} from '@opensdks/runtime'
    import {salesloftSdkDef} from '@opensdks/sdk-salesloft'

    const salesloft = initSDK(salesloftSdkDef, {
      auth: {
        // for provider-specific auth
        bearer: `${process.env['SALESLOFT_TOKEN']}`,
        // or for passthrough auth via OPENINT_API_KEY & end user resourceId
        openInt: {
          apiKey: `${process.env['OPENINT_API_KEY']}`,
          resourceId: END_USER_RESOURCE_ID,
        }
      }, params: { ... }
    })

    // List account stages
    const res = await salesloft.GET('/v2/account_stages.json')
```
